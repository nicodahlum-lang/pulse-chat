{
  "name": "pulse-chat",
  "private": true,
  "version": "1.0.0",
  "description": "Pulse Chat is a Discord-like local chat app with text channels, voice rooms, and server groups.",
  "scripts": {
    "bootstrap": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "check": "npm --prefix backend test && npm --prefix frontend run typecheck && npm --prefix frontend run build",
    "seed": "npm --prefix backend run seed",
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
