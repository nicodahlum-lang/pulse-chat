{
  "name": "pulse-chat-backend",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Local Express and Socket.IO backend for Pulse Chat.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "seed": "node src/seed.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
